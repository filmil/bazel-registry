matrix:
  platform:
  - debian10
  - ubuntu2004
  bazel:
  - 8.1.1
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@bazel_rules_bid//...'
